# Runtime dependencies for app/streamlit_app.py -- this is what Streamlit
# Community Cloud installs (it reads requirements.txt by default). Kept
# deliberately lean: dbt and the synthetic-data generator are NOT needed
# here, since the app only READS the prebuilt warehouse.duckdb and
# rag/vectorstore/ committed to the repo -- see requirements-pipeline.txt
# for what's needed to *regenerate* those locally.
duckdb==1.1.3
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
faiss-cpu==1.9.0.post1
langchain-community==0.3.9
langchain-anthropic==0.3.1
langchain-openai==0.2.10
streamlit==1.40.2
python-dotenv==1.0.1
